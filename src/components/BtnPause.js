import React, { Component } from "react";

class BtnPause extends Component {
    render() {
        return(
            <button className="App-btn">
                Pause
            </button>
        );
    };
};

export default BtnPause;